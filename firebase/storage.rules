rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // DEMO MODE: Allow all read/write for testing
    // WARNING: Do not use in production!
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
